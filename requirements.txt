cvzone==1.5.6
ultralytics==8.0.26
hydra-core==1.3.2  # Latest version, compatible with PyYAML 6.x
matplotlib==3.8.0  # Latest stable version
numpy==1.25.2  # Latest, should be compatible with OpenCV, Torch, etc.
opencv-python==4.8.0.76  # Latest version, compatible with cvzone
Pillow==10.0.1  # Latest, compatible with Torch and OpenCV
PyYAML==6.0  # Latest, works with hydra-core 1.3.2
requests==2.31.0  # Latest, no conflicts expected
scipy==1.11.2  # Latest, compatible with numpy 1.25.x
torch==2.0.1  # Latest, compatible with the latest torchvision
torchvision==0.15.2  # Latest, matches torch 2.0.x
tqdm==4.66.1  # Latest stable version, no conflicts
filterpy==1.4.5  # No updates, this is still the latest version
scikit-image==0.21.0  # Latest, compatible with numpy 1.25.x
#lap==0.4.0  # No newer version, compatible with others
